(ns wolfie.data
  (:require [clojure.core.async :as async]))


(defn create-new-drawing!
  [data]
  )


(defn set-button-drawing 
  [btn drawing-id]
  )


(defn get-button-config
  []
  (prn "get button config")
  )


(defn get-drawings
  []
  )
